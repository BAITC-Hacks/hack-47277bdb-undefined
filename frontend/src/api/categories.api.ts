import apiClient, { requestData } from "./client"; import { categories } from "../mocks/mock-data"; import { mockResponse, useMocks } from "../mocks/mock-api"; import type { Category } from "../types/category.types";
export const categoriesApi = { list: () => useMocks ? mockResponse(categories) : requestData<Category[]>(apiClient.get("/categories")), tree: () => useMocks ? mockResponse(categories) : requestData<Category[]>(apiClient.get("/categories/tree")), get: (slug: string) => useMocks ? mockResponse(categories.find((category) => category.slug === slug)!) : requestData<Category>(apiClient.get(`/categories/${slug}`)) };
